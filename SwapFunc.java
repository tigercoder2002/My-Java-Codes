package Confidential_Package;
public class SwapFunc{
    public static void swap(Byte x,Byte y){
        Byte temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Short x,Short y){
        Short temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Integer x,Integer y){
        Integer temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Long x,Long y){
        Long temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Float x,Float y){
        Float temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Double x,Double y){
        Double temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Character x,Character y){
        Character temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(Boolean x,Boolean y){
        Boolean temp;
        temp=x; x=y;    y=temp;
    }
    public static void swap(String x,String y){
    String temp;
    temp=x; x=y;    y=temp;
}
    public static void swap(StringBuffer x,StringBuffer y){
        StringBuffer temp;
        temp=x; x=y;    y=temp;
    }
}